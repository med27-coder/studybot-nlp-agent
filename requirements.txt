# StudyBot — Python Dependencies
# Install with: pip install -r requirements.txt
#
# Note for Windows ARM64 (Python 3.13) — if aiohttp fails to build, use:
#   pip install -r requirements.txt --no-deps
#   pip install pydantic httpx tenacity PyYAML requests

langchain>=0.3.0
langgraph>=0.2.0
langchain-ollama>=0.2.0

# -------------------------------------------------------------------
# AI Model (runs locally via Ollama — no API key needed)
# -------------------------------------------------------------------
# This project was built and tested using Llama 3.2 by Meta.
# Download it with: ollama pull llama3.2
# Model page: https://ollama.com/library/llama3.2
#
# You can also use Gemma 3 by Google as an alternative.
# Download it with: ollama pull gemma3:4b
# Model page: https://ollama.com/library/gemma3
#
# To switch models, change this line in the notebook (Cell 2):
#   OLLAMA_MODEL = "llama3.2"   ← default used in this project
#   OLLAMA_MODEL = "gemma3:4b"  ← Google's Gemma 3 alternative
# -------------------------------------------------------------------
